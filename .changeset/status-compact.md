---
"devsess": minor
---

Add `-s` for `--service`; `status -a` shows all active runs in full detail, while `status` summarizes other projects in one line each. Finished runs remain available through the `Last run` summary when nothing is active.
