---
'devsess': minor
---

Initial release: Effect-based dev-session scaffolding (`defineDevCli`) with persistent sessions, sticky ports, managed subprocesses, and cross-service running signals; a per-session PGlite + Drizzle adapter at `devsess/pglite`; and a plain-async facade at `devsess/async` for scripts that prefer Promises over Effect.
