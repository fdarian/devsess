---
'devsess': minor
---

Initial release: dev-session CLI scaffolding (`defineDevCli`) with persistent sessions, sticky ports, managed subprocesses, and cross-service running signals — plus a per-session PGlite + Drizzle adapter (`devsess/pglite`) for preparing local databases from your migrations.
